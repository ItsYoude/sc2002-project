package utility;

public enum LevelType {
    BASIC,
    INTERMEDIATE,
    ADVANCED;


    // public static LevelType fromString(String s) {
    //     return LevelType.valueOf(s.trim().toUpperCase());
    // }

    // public static LevelType fromYear(int year) {
    //     if (year <= 2) return BASIC;
    //     return INTERMEDIATE; // or ADVANCED depending on your logic
    // }
}